[project]
name = "freelancerescrowpaymentbackend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=6.0.5",
    "django-environ>=0.13.0",
]

[dependency-groups]
production = [
    "psycopg>=3.3.4",
    "uvicorn>=0.47.0",
]
